namespace PropertyManager.Models
{
    /// <summary>
    /// Defines how often rent increases are applied per agreement.
    /// </summary>
    public enum IncreaseType
    {
        Monthly = 0,
        Yearly = 1,
        CustomMonths = 2
    }
}
